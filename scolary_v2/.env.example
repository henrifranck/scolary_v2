# --- Project Metadata ---
PROJECT_NAME='Scolary_v2'
IMAGE_NAME='app'

# --- Networking Configuration ---
HOST_PORT='8081'
CONTAINER_PORT='8081'

# --- CORS Settings ---
# Comma-separated list of frontend origins allowed to access the backend
# Example for local development with React/Vue/Angular default ports
BACKEND_CORS_ORIGINS=["http://localhost", "http://localhost:4200", "http://localhost:5173"]

# --- MySQL Database Configuration ---
MYSQL_HOST='host.docker.internal'  # Or the Docker service name (e.g., db, host.docker.internal) or external host IP
MYSQL_PORT=3306
MYSQL_USER='scolary_user'
MYSQL_PASSWORD='Password12345'
MYSQL_DATABASE='scolary_db'

# --- Superuser Credentials ---
FIRST_SUPERUSER='admin@scolary-user.com'
LAST_NAME_SUPERUSER='string'
FIRST_NAME_SUPERUSER='string'
FIRST_SUPERUSER_PASSWORD='Password1234'  # Change this for production!

# --- App Configuration ---
API_V1_STR='/api/v1'
SECRET_KEY='tzrctxhgdc876guyguv6v'  # Generate a strong key, e.g. openssl rand -hex 32
ENVIRONMENT='development'

# --- MongoDB Settings ---
MONGO_DB_HOST='host.docker.internal'
MONGO_DB_PORT=27017
MONGO_DB_USER='root'
MONGO_DB_PASSWORD='password'
MONGO_DB_CLIENT='admin'
MONGO_DB_COLLECTION='investissement'

# -- Test ---
TESTING=0
